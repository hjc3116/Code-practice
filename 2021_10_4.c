#include <stdio.h>

int main()
{
/*
	int x;
	int n = 0;
	
	scanf_s("%d", &x);
	while (x > 0) {
		n++;
		x = x / 10;
	}
							// printf("x=%d,n=%d",x.n)   调试 注意 特殊值：10的倍数，0 ，负数
	printf("%d", n);
*/
	
/*
	int x;
	int y = 0;

	scanf_s("%d",&x); 
	int t = x; 				//使用另一个变量保存原始值
	while( x > 1 ) {
		x /= 2;
		y ++;
	}
	printf("log2 of %d is %d.",t,y)
*/
	
	return 0;
}

